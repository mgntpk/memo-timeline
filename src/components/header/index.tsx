import styles from "./.module.css";

const header = () => {
    return (
        <>
            <header>
                <h1 className={styles.h1}>memo-timeline</h1>
            </header>
        </>
    );
};

export default header;
